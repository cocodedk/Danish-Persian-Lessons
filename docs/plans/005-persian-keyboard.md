# Plan 005 — On-Screen Persian Keyboard (stub — Fable will detail before execution)

Depends on: 004.

Scope preview: a React on-screen Persian keyboard component (no OS keyboard dependency) for typing
exercises. Simplified ISIRI-inspired layout, letters + ZWNJ + backspace first (no shift layers in v1),
keys ≥ 44×44px in the bottom thumb zone, keypress feedback per ART-DIRECTION motion rules.
Typing exercise: shown the Danish word + audio-less prompt, type the Persian word; diff rendered
with red teacher marks. Unit tests for layout map and input buffer (ZWNJ handling!).
Capstone exercise: type your own name (`profile.faSpelling` from plan 006) — finish with warm
teacher praise («آفرین، سارا!» / "Flot, Sara!").

## Questions
(add here)
