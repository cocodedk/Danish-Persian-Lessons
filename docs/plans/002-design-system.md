# Plan 002 — Design System Components (stub — Fable will detail before execution)

Depends on: 001. Source of truth: `docs/design/ART-DIRECTION.md`.

Scope preview: the notebook-page component kit — `RuledSection` (blue ruling + red margin line,
RTL-mirrored), `Button` (pen-blue), `VowelChip` (red mark, used by lesson 1), `ProgressTick`
(red margin checkmarks), typography components for Persian specimens vs body. Storybook-style
gallery route `#/kit` for visual review. Dark scheme + reduced motion verified per component.

## Questions
(add here)
